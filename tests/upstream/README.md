# Pinned test interface

`teraflow-v7/_Driver.py` is copied without modification from
`src/device/service/driver_api/_Driver.py` in
[ETSI TeraFlow SDN](https://labs.etsi.org/rep/tfs/controller), tag v7.0.0,
commit `fb8707871eba26806cac7ac373c70b2bb5bd26fc`.
Its Apache 2.0 notice and repository LICENSE are retained, with byte hashes.
The test loader supplies package namespaces and loads this actual interface;
it does not start, emulate or replace the TeraFlow controller services.
